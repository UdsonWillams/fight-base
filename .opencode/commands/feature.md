---
description: Implement a new feature following TDD with architecture standards (view > service > repository, schemas by domain, pytest only)
---

$ARGUMENTS

Load the feature skill and follow the TDD workflow strictly: plan, red (failing test), green (implementation), refactor.

Follow architecture standards: view > service > repository, schemas in app/schemas/<domain>/, API versioned in app/api/v1/, pytest-only tests with Given/When/Then docstrings. If feature involves Azure, security, or KQL, also load the relevant domain skill.
